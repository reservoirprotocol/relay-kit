{
  "name": "relay",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "demo": "pnpm run --cwd demo dev",
    "build:sdk": "pnpm run --cwd packages/sdk build",
    "syncSdkTypes": "pnpm run --cwd packages/sdk ./scripts/sync-api.mjs",
    "deploy": "pnpm build:sdk && pnpm run --cwd packages/sdk deploy",
    "sdk": "pnpm run --cwd packages/sdk dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "openapi-typescript": "^6.7.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
